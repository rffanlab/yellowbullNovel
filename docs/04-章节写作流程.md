# 04 - 章节写作流程

> 对应 inkos 的 `WriterAgent` + `Settler`（Observer + Reflector 双 Agent）。
> 参考源码：`inkos/packages/core/src/agents/writer.ts`、`writer-prompts.ts`、`writer-parser.ts`、`settler-prompts.ts`、`settler-parser.ts`、`settler-delta-parser.ts`、`observer-prompts.ts`、`post-write-validator.ts`。

---

## 目录

- [一、流程概述](#一流程概述)
- [二、入口函数签名](#二入口函数签名)
- [三、WriterAgent 设计](#三writeragent-设计)
  - [3.1 输入：WriteChapterInput](#31-输入writechapterinput)
  - [3.2 输出：WriteChapterOutput](#32-输出writechapteroutput)
  - [3.3 步骤1：加载全部上下文](#33-步骤1加载全部上下文)
  - [3.4 步骤2：构建写作 system prompt](#34-步骤2构建写作-system-prompt)
  - [3.5 步骤3：LLM 生成章节正文](#35-步骤3llm-生成章节正文)
  - [3.6 步骤4：parseCreativeOutput 解析标题+正文](#36-步骤4parsecreativeoutput-解析标题正文)
- [四、Settler 状态结算（双 Agent）](#四settler-状态结算双-agent)
  - [4.1 Observer：事实提取](#41-observer事实提取)
  - [4.2 Reflector：状态回写](#42-reflector状态回写)
  - [4.3 解析结算输出](#43-解析结算输出)
- [五、写后校验 validatePostWrite](#五写后校验-validatepostwrite)
- [六、完整 Go 接口定义](#六完整-go-接口定义)
- [七、LLM prompt 核心段落（中文版）](#七llm-prompt-核心段落中文版)

---

## 一、流程概述

章节写作是写作流水线的核心阶段（Phase 4），在上下文组装（Phase 3.5）之后、章节审查循环（Phase 5）之前。WriterAgent 负责生成章节正文 + 结算运行时状态，分两大阶段：

1. **Phase 1 创作正文**（temperature 0.7）：加载上下文 → 构建 system prompt → LLM 生成 → 解析标题+正文
2. **Phase 2 状态结算**（temperature 0.3）：Observer 提取事实 → Reflector 生成 RuntimeStateDelta → 更新 truth files

```
WriteChapterInput
  │  book / bookDir / chapterNumber
  │  chapterIntent / chapterMemo / contextPackage / ruleStack / lengthSpec
  ▼
┌─────────────────────────────────────────────┐
│ 步骤1: 加载全部上下文                          │
│  (13 个 truth files + 近章正文 + 题材规则)     │
│  → storyBible/volumeOutline/currentState     │
│    /hooks/summaries/subplots/arcs/matrix     │
│    /styleGuide/parentCanon/fanficCanon       │
│    + recentChapters(对话指纹)                 │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ 步骤2: buildWriterSystemPrompt               │
│  角色设定 + 输入治理契约 + memo对齐契约        │
│  + 字数指导 + 写作工艺卡 + 散文执行规则        │
│  + 创作宪法 + 沉浸支柱 + 黄金开篇纪律          │
│  + 题材规则 + 主角规则 + 叙事人称 + 输出格式   │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ 步骤3: LLM 生成 (temperature=0.7, 流式)       │
│  system: creativeSystemPrompt                │
│  user: governedUserPrompt / legacyUserPrompt │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ 步骤4: parseCreativeOutput                    │
│  提取 PRE_WRITE_CHECK / CHAPTER_TITLE         │
│       / CHAPTER_CONTENT 三个区块              │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ Phase 2: Settler 状态结算                     │
│  ┌──────────────┐    ┌──────────────────┐   │
│  │ Observer     │───▶│ Reflector        │   │
│  │ (temp=0.5)   │    │ (temp=0.3)       │   │
│  │ 提取9类事实  │    │ 生成 Delta JSON  │   │
│  └──────────────┘    └────────┬─────────┘   │
│                               ▼              │
│                    parseSettlerDeltaOutput    │
│                    → RuntimeStateDelta        │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ 写后校验 validatePostWrite (零 LLM 成本)      │
│  + detectCrossChapterRepetition              │
│  + detectParagraphLengthDrift                │
│  + analyzeAITells                            │
│  + analyzeHookHealth                         │
└─────────────────┬───────────────────────────┘
                  ▼
          WriteChapterOutput
```

**核心设计原则**：
1. **创作与结算分离**：Phase 1 只写正文（creative mode），Phase 2 只做状态结算，互不干扰
2. **双 Agent 结算**：Observer 先提取事实（宁多勿少），Reflector 再做增量更新（交叉验证）
3. **零 LLM 成本校验**：写后校验全部是正则/规则检测，不消耗 token
4. **治理模式优先**：当 chapterMemo + contextPackage + ruleStack 三者齐备时走 governed 路径，否则走 legacy 路径
5. **RuntimeStateDelta 优先**：结算输出优先走 Delta JSON 路径（结构化增量），失败时回退到 Markdown 全量输出

---

## 二、入口函数签名

```go
// WriteChapter 是章节写作流程的入口。
// 完成 Phase 1（创作正文）+ Phase 2（状态结算）+ 写后校验，返回 WriteChapterOutput。
//
// 参数：
//   - input: 写作输入（book/bookDir/chapterNumber + 治理包）
//
// 返回：
//   - *WriteChapterOutput: 含 title/content/wordCount/runtimeStateDelta/postWriteErrors
//   - error
func (a *WriterAgent) WriteChapter(ctx context.Context, input *WriteChapterInput) (*WriteChapterOutput, error)

// SettleChapterState 是单独触发状态结算的入口（不重新生成正文）。
// 用于外部已有正文、只需结算状态的场景。
func (a *WriterAgent) SettleChapterState(ctx context.Context, input *SettleChapterStateInput) (*WriteChapterOutput, error)

// SaveChapter 将 WriteChapterOutput 持久化到磁盘。
// 写入 chapters/0001_标题.md + story/ 下的 truth files。
func (a *WriterAgent) SaveChapter(bookDir string, output *WriteChapterOutput, numericalSystem bool, language Language) error
```

---

## 三、WriterAgent 设计

### 3.1 输入：WriteChapterInput

```go
// WriteChapterInput 章节写作输入
type WriteChapterInput struct {
    Book               *BookConfig             // 书籍配置（标题/题材/平台/字数目标等）
    BookDir            string                  // 书籍根目录路径
    ChapterNumber      int                     // 当前章节号（从1开始）
    ExternalContext    string                  // 外部指令（最高优先级，覆盖卷纲）
    ChapterIntent      string                  // 章节意图（governed 模式）
    ChapterMemo        *ChapterMemo            // 章节备忘（7段对齐）
    ChapterIntentData  *ChapterIntent          // 解析后的意图结构
    ContextPackage     *ContextPackage         // 上下文包（选定证据）
    RuleStack          *RuleStack              // 规则栈（4层优先级）
    LengthSpec         *LengthSpec             // 字数规格（目标/软区间/硬区间）
    WordCountOverride  int                     // 字数覆盖（>0 时覆盖 LengthSpec.target）
    TemperatureOverride float64                // 温度覆盖（默认0.7）
}
```

### 3.2 输出：WriteChapterOutput

```go
// WriteChapterOutput 章节写作输出
type WriteChapterOutput struct {
    ChapterNumber          int                       // 章节号
    Title                  string                    // 章节标题
    Content                string                    // 正文（经表面归一化）
    WordCount              int                       // 字数（归一化后）
    PreWriteCheck          string                    // 写作自检表（LLM 输出）
    PostSettlement         string                    // 结算摘要
    RuntimeStateDelta      *RuntimeStateDelta        // 运行时状态增量（结构化）
    RuntimeStateSnapshot   *RuntimeStateSnapshot     // 运行时状态快照（结算后）
    UpdatedState           string                    // 更新后的状态卡 Markdown
    UpdatedLedger          string                    // 更新后的资源账本 Markdown
    UpdatedHooks           string                    // 更新后的伏笔池 Markdown
    ChapterSummary         string                    // 本章摘要行（表格行）
    UpdatedChapterSummaries string                   // 更新后的完整章节摘要
    UpdatedSubplots        string                    // 更新后的支线进度板
    UpdatedEmotionalArcs   string                    // 更新后的情感弧线
    UpdatedCharacterMatrix string                    // 更新后的角色交互矩阵
    PostWriteErrors        []PostWriteViolation      // 写后校验错误
    PostWriteWarnings      []PostWriteViolation      // 写后校验警告
    HookHealthIssues       []HookHealthIssue         // 伏笔健康问题
    TokenUsage             *TokenUsage               // token 用量
}

// TokenUsage token 用量统计
type TokenUsage struct {
    PromptTokens     int
    CompletionTokens int
    TotalTokens      int
}
```

### 3.3 步骤1：加载全部上下文

WriterAgent 并行加载 13 个 truth files + 近章正文 + 题材规则。

```go
// 步骤1: 加载全部上下文
func (a *WriterAgent) loadAllContext(ctx context.Context, input *WriteChapterInput) (*writerContext, error) {
    bookDir := input.BookDir
    placeholder := "(文件尚未创建)"

    // 13 个 truth files 并行读取
    storyBible, volumeOutline, styleGuide, currentState, ledger, hooks,
    chapterSummaries, subplotBoard, emotionalArcs, characterMatrix, styleProfileRaw,
    parentCanon, fanficCanonRaw := parallelRead13Files(bookDir, placeholder)

    // 近章正文：默认1章用于衔接，5章用于对话指纹提取
    recentChapters := a.loadRecentChapters(bookDir, input.ChapterNumber, 1)
    fingerprintChapters := a.loadRecentChapters(bookDir, input.ChapterNumber, 5)

    // 题材规则 + 书籍规则
    genreProfile, genreBody := readGenreProfile(a.projectRoot, input.Book.Genre)
    bookRules, bookRulesBody := readBookRules(bookDir)

    // 文风指纹（从 style_profile.json 提取）
    styleFingerprint := buildStyleFingerprint(styleProfileRaw)

    // 对话指纹（从近5章正文提取每个角色的说话风格）
    dialogueFingerprints := extractDialogueFingerprints(fingerprintChapters, storyBible)

    // 相关历史摘要（从卷纲提取角色名/hookID，匹配章节摘要）
    relevantSummaries := findRelevantSummaries(chapterSummaries, volumeOutline, input.ChapterNumber)

    return &writerContext{...}, nil
}
```

**13 个 truth files 清单**：

| # | 变量名 | 文件路径 | 用途 |
|---|--------|----------|------|
| 1 | storyBible | `story_frame.md`（readStoryFrame） | 世界观/主线/卷纲 |
| 2 | volumeOutline | `volume_map.md`（readVolumeMap） | 当前卷的章节大纲 |
| 3 | styleGuide | `story/style_guide.md` | 文风指南 |
| 4 | currentState | `story/current_state.md`（readCurrentStateWithFallback） | 当前状态卡 |
| 5 | ledger | `story/particle_ledger.md` | 资源账本 |
| 6 | hooks | `story/pending_hooks.md` | 伏笔池 |
| 7 | chapterSummaries | `story/chapter_summaries.md` | 全部历史章节摘要 |
| 8 | subplotBoard | `story/subplot_board.md` | 支线进度板 |
| 9 | emotionalArcs | `story/emotional_arcs.md` | 情感弧线 |
| 10 | characterMatrix | `roles/` 目录（readCharacterContext） | 角色交互矩阵 |
| 11 | styleProfileRaw | `story/style_profile.json` | 文风指纹数据 |
| 12 | parentCanon | `story/parent_canon.md` | 正传正典（番外专用） |
| 13 | fanficCanonRaw | `story/fanfic_canon.md` | 同人正典（同人专用） |

**注意**：`readCurrentStateWithFallback` 在 `current_state.md` 仅为种子占位时，从 `roles/*.Current_State` + `pending_hooks` 中 startChapter=0 的行推导初始状态，确保 writer 看到实质内容。

### 3.4 步骤2：构建写作 system prompt

`buildWriterSystemPrompt` 按顺序拼接以下段落。governed 模式（chapterMemo 存在时）会额外插入输入治理契约和 memo 对齐契约。

```go
// buildWriterSystemPrompt 构建写作 system prompt
func buildWriterSystemPrompt(
    book *BookConfig,
    genreProfile *GenreProfile,
    bookRules *BookRules,
    bookRulesBody string,
    genreBody string,
    styleGuide string,
    styleFingerprint string,
    chapterNumber int,
    mode WriterMode, // "creative" | "full"
    fanficContext *FanficContext,
    language Language,
    inputProfile InputProfile, // "governed" | "legacy"
    lengthSpec *LengthSpec,
) string {
    sections := []string{
        buildGenreIntro(book, genreProfile),                    // 1. 角色设定+题材底色
        buildCoreRules(lengthSpec),                             // 2. 核心规则（含人物铁律/叙事技法/看点密度/断章/逻辑/语言/去AI味/禁令）
        buildGovernedInputContract(language, governed),         // 3. 输入治理契约（governed 时才有）
        buildChapterMemoContract(language, governed),           // 4. Chapter Memo 对齐契约（7段）
        buildLengthGuidance(lengthSpec, language),              // 5. 字数指导
        buildWritingCraftCard(language),                        // 6. 写作工艺卡（v10 精简版）
        buildProseExecutionRules(language),                     // 7. 散文执行规则（明喻节制+高潮演出）
        buildCreativeConstitution(language),                    // 8. 创作宪法（14条原则）
        buildImmersionPillars(language),                        // 9. 沉浸支柱（六根支柱）
        buildGoldenOpeningDiscipline(chapterNumber, language),  // 10. 黄金开篇纪律（仅 chapterNumber<=3）
        buildGoldenChaptersRules(chapterNumber, language),      // 11. 黄金N章特殊指令
        buildFullCastTrackingIfEnabled(bookRules),              // 12. 全员追踪（可选）
        buildGenreRules(genreProfile, genreBody),               // 13. 题材规则
        buildProtagonistRules(bookRules),                       // 14. 主角规则
        buildNarrativePersonRule(bookRules, language),          // 15. 叙事人称规则
        buildBookRulesBody(bookRulesBody),                      // 16. 本书专属规则
        buildStyleGuide(styleGuide),                            // 17. 文风指南
        buildStyleFingerprintSection(styleFingerprint),         // 18. 文风指纹
        buildFanficSectionsIfPresent(fanficContext),            // 19-21. 同人相关（可选）
        buildOutputFormat(book, genreProfile, lengthSpec, mode),// 22. 输出格式
    }
    return joinNonEmpty(sections, "\n\n")
}
```

**各段落说明**：

| # | 段落 | 来源函数 | 说明 |
|---|------|----------|------|
| 1 | 角色设定 | `buildGenreIntro` | "你是一位专业的{题材}网络小说作家。你为{平台}平台写作。" |
| 2 | 核心规则 | `buildCoreRules` | 25+条硬规则：手机阅读节奏/伏笔呼应/人设铁律/叙事技法/看点密度(每300字1爽点)/80-20断章/逻辑自洽/语言约束/去AI味/硬性禁令 |
| 3 | 输入治理契约 | `buildGovernedInputContract` | governed 模式：chapter intent 优先于卷纲；硬护栏不可突破；Hook Debt 原文片段必须用 |
| 4 | Memo 对齐契约 | `buildChapterMemoContract` | 7段对齐：当前任务/读者在等什么/该兑现的·暂不掀的/日常过渡功能/三连问/章尾改变/hook账/不要做 |
| 5 | 字数指导 | `buildLengthGuidance` | 目标字数/允许区间/硬区间 |
| 6 | 写作工艺卡 | `buildWritingCraftCard` | v10精简版：情绪外化/盐溶于汤/配角/五感/具体化/句式/欲望驱动/人设三问/对话/禁止/升级/周期意识/高潮后影响/期待管理/信息边界 |
| 7 | 散文执行规则 | `buildProseExecutionRules` | 明喻节制（每场景≤1处）+ 高潮必须演出不许概述 |
| 8 | 创作宪法 | `buildCreativeConstitution` | 14条原则散文式表述：show don't tell/盐溶于汤/三腿立足/配角账本/节奏呼吸/章尾钩子/全员智商/时间线/日常七成伏笔/关系事件驱动/人设一致/场景非总结/拒绝流水账 |
| 9 | 沉浸支柱 | `buildImmersionPillars` | 六根支柱：基础标签化/可视化熟悉感/共鸣两层/欲望两腿/五感钩子/人设反差 |
| 10 | 黄金开篇纪律 | `buildGoldenOpeningDiscipline` | 仅 chapterNumber<=3 触发：第1章800字内触发主线冲突/前300字最后一句必须反转/第2章金手指做出来/第3章短期目标浮出 |
| 11 | 黄金N章 | `buildGoldenChaptersRules` | 中文3章/英文5章的逐章特殊指令 |
| 12 | 全员追踪 | `buildFullCastTracking` | enableFullCastTracking 时触发 |
| 13 | 题材规则 | `buildGenreRules` | 高疲劳词/节奏规则/章节类型 |
| 14 | 主角规则 | `buildProtagonistRules` | 性格锁定/行为约束/本书禁忌/风格禁区 |
| 15 | 叙事人称 | `buildNarrativePersonRule` | bookRules.narrativePerson 显式设定时才约束 |
| 16 | 本书专属规则 | `buildBookRulesBody` | 用户自定义 markdown |
| 17 | 文风指南 | `buildStyleGuide` | style_guide.md 内容 |
| 18 | 文风指纹 | `buildStyleFingerprintSection` | 平均句长/段落长度/词汇多样性/高频句式 |
| 19-21 | 同人 | `buildFanficSections` | 正典参照/角色声纹/模式指令 |
| 22 | 输出格式 | `buildOutputFormat` | creative 模式只输出3区块；full 模式输出全部区块 |

### 3.5 步骤3：LLM 生成章节正文

```go
// Phase 1: 创作正文（temperature 0.7）
func (a *WriterAgent) generateCreative(ctx context.Context, sysPrompt, userPrompt string, temp float64) (*llmResponse, error) {
    messages := []Message{
        {Role: "system", Content: sysPrompt},
        {Role: "user", Content: userPrompt},
    }
    // chat 接口支持流式回调，writer 可在生成过程中实时输出
    return a.chat(ctx, messages, ChatOptions{
        Temperature: temp,
        Stream:      true,
        OnChunk:     a.streamCallback, // 可选：流式输出回调
    })
}
```

**user prompt 分两种路径**：

1. **governed 路径**（chapterMemo + contextPackage + ruleStack 三者齐备）：
   - 用户方向块（author_intent / current_focus 优先）
   - chapter_memo 叙事块（7段）
   - 已选上下文（ContextPackage.selectedContext 渲染）
   - 规则栈（硬护栏/软约束/诊断规则）
   - 字数要求

2. **legacy 路径**（三者缺失任一）：
   - 当前状态卡
   - 资源账本
   - 伏笔池（POV 过滤后）
   - 章节摘要 / 支线 / 情感弧线 / 角色矩阵
   - 对话指纹
   - 相关历史摘要
   - 正传正典（番外时）
   - 近章正文
   - 世界观设定
   - 字数要求

### 3.6 步骤4：parseCreativeOutput 解析标题+正文

```go
// parseCreativeOutput 解析 LLM 创作输出
// 提取 === PRE_WRITE_CHECK === / === CHAPTER_TITLE === / === CHAPTER_CONTENT === 三个区块
func parseCreativeOutput(chapterNumber int, content string, countingMode LengthCountingMode) *CreativeOutput {
    extract := func(tag string) string {
        // 正则: === TAG === \s* (内容) (?==== [A-Z_]+ ===|$)
        re := regexp.MustCompile(fmt.Sprintf(`=== %s ===\s*([\s\S]*?)(?==== [A-Z_]+ ===|$)`, tag))
        match := re.FindStringSubmatch(content)
        if len(match) > 1 {
            return strings.TrimSpace(match[1])
        }
        return ""
    }

    chapterContent := extract("CHAPTER_CONTENT")
    // 回退策略：小模型可能不输出 === TAG === 标记
    if chapterContent == "" {
        chapterContent = fallbackExtractContent(content, countingMode)
    }

    title := extract("CHAPTER_TITLE")
    if title == "" {
        title = fallbackExtractTitle(content, chapterNumber, countingMode)
    }

    return &CreativeOutput{
        Title:         title,
        Content:       chapterContent,
        WordCount:     countChapterLength(chapterContent, countingMode),
        PreWriteCheck: extract("PRE_WRITE_CHECK"),
    }
}
```

**回退策略**（应对小模型不输出标记）：
- `fallbackExtractContent`：尝试 `# 第N章` 标题 → `正文：` 标签 → 过滤元数据行后取最长 prose block（>100字）
- `fallbackExtractTitle`：尝试 `# 第N章 标题` → `章节标题：` 标签 → 默认 `第N章`

---

## 四、Settler 状态结算（双 Agent）

Settler 采用 Observer-Reflector 双 Agent 架构：

1. **Observer**（temperature 0.5）：从章节正文提取 9 类事实，宁多勿少
2. **Reflector**（temperature 0.3）：基于观察日志 + 当前 truth files → 生成 RuntimeStateDelta（JSON 增量）

### 4.1 Observer：事实提取

```go
// buildObserverSystemPrompt 构建 Observer system prompt
// 提取9类事实：角色行为/位置变化/资源变化/关系变化/情绪变化/信息流动/剧情线索/时间推进/身体状态
func buildObserverSystemPrompt(book *BookConfig, gp *GenreProfile, language Language) string {
    // 规则：只从正文提取——不推测；宁多勿少；具体化；记录时间标记；标注在场角色
    // 输出格式：=== OBSERVATIONS ===
    //   [角色行为] / [位置变化] / [资源变化] / [关系变化]
    //   [情绪变化] / [信息流动] / [剧情线索] / [时间] / [身体状态]
}

// Observer 调用
func (a *WriterAgent) observe(ctx context.Context, chapterNumber int, title, content string, language Language) (string, error) {
    sysPrompt := buildObserverSystemPrompt(book, genreProfile, language)
    userPrompt := buildObserverUserPrompt(chapterNumber, title, content, language)
    resp := a.chat(ctx, []Message{
        {Role: "system", Content: sysPrompt},
        {Role: "user", Content: userPrompt},
    }, ChatOptions{Temperature: 0.5})
    return resp.Content, nil // 观察日志原文
}
```

### 4.2 Reflector：状态回写

```go
// buildSettlerSystemPrompt 构建 Reflector system prompt
func buildSettlerSystemPrompt(book *BookConfig, gp *GenreProfile, bookRules *BookRules, language Language) string {
    // 角色设定：你是状态追踪分析师
    // 工作模式：1.提取状态变化 2.增量更新 3.严格 === TAG === 格式
    // 分析维度：角色出场/位置/物品/伏笔/情感/支线/关系
    // 伏笔追踪规则：新增/提及/推进/回收/延后 的判定标准
    // 铁律：只记录正文中实际发生的事，不推断/不预测/不从卷纲补充
    // 输出格式：POST_SETTLEMENT + RUNTIME_STATE_DELTA(JSON)
}

// buildSettlerUserPrompt 构建 Reflector user prompt
// 包含：观察日志 + 正文 + 当前状态卡 + 账本 + 伏笔池 + 摘要 + 支线 + 情感 + 矩阵 + 卷纲
func buildSettlerUserPrompt(params SettlerUserPromptParams) string {
    // 结构：
    // ## 观察日志（由 Observer 提取）
    // ## 本章正文
    // ## 当前状态卡 / 资源账本 / 伏笔池 / 章节摘要 / 支线 / 情感 / 矩阵
    // ## 卷纲（governed 模式时替换为控制输入块）
    // 请严格按照 === TAG === 格式输出结算结果。
}

// Reflector 调用
func (a *WriterAgent) reflect(ctx context.Context, params settleParams) (*settleResult, error) {
    // Phase 2a: Observer 提取事实
    observations := a.observe(ctx, params.chapterNumber, params.title, params.content, resolvedLang)

    // Phase 2b: Reflector 回写
    settlerSystem := buildSettlerSystemPrompt(params.book, params.genreProfile, params.bookRules, resolvedLang)
    settlerUser := buildSettlerUserPrompt(SettlerUserPromptParams{
        ChapterNumber:       params.chapterNumber,
        Title:               params.title,
        Content:             params.content,
        CurrentState:        capLegacy("current_state", params.currentState, 7000),
        Ledger:              capLegacy("particle_ledger", params.ledger, 6000),
        Hooks:               capLegacy("pending_hooks", params.hooks, 9000),
        ChapterSummaries:    capLegacy("chapter_summaries", params.chapterSummaries, 9000),
        SubplotBoard:        capLegacy("subplot_board", params.subplotBoard, 7000),
        EmotionalArcs:       capLegacy("emotional_arcs", params.emotionalArcs, 7000),
        CharacterMatrix:     capLegacy("character_matrix", params.characterMatrix, 12000),
        VolumeOutline:       capLegacy("volume_outline", params.volumeOutline, 12000),
        Observations:        observations,
        SelectedEvidenceBlock: params.selectedEvidenceBlock,
        GovernedControlBlock: params.governedControlBlock,
        ValidationFeedback:  params.validationFeedback,
    })

    resp := a.chat(ctx, []Message{
        {Role: "system", Content: settlerSystem},
        {Role: "user", Content: settlerUser},
    }, ChatOptions{Temperature: 0.3})

    return a.parseSettleResult(resp.Content, params)
}
```

### 4.3 解析结算输出

结算输出有两条解析路径，**Delta JSON 优先**，失败时回退到 Markdown 全量：

```go
func (a *WriterAgent) parseSettleResult(content string, params settleParams) (*settleResult, error) {
    // 路径1: 优先尝试解析 RUNTIME_STATE_DELTA (JSON 增量)
    deltaOutput, err := parseSettlerDeltaOutput(content)
    if err == nil {
        return &settleResult{
            postSettlement:    deltaOutput.PostSettlement,
            runtimeStateDelta: deltaOutput.RuntimeStateDelta,
            // Delta 模式下 truth files 由 buildRuntimeStateArtifacts 重建
        }, nil
    }

    // 路径2: 回退到 Markdown 全量解析（parseSettlementOutput）
    settlement := parseSettlementOutput(content, params.genreProfile)
    // governed 模式下合并旧表（按 key 去重合并）
    if params.governedControlBlock != "" {
        settlement.UpdatedHooks = mergeTableMarkdownByKey(params.originalHooks, settlement.UpdatedHooks, []int{0})
        settlement.UpdatedSubplots = mergeTableMarkdownByKey(params.originalSubplots, settlement.UpdatedSubplots, []int{0})
        settlement.UpdatedEmotionalArcs = mergeTableMarkdownByKey(params.originalEmotionalArcs, settlement.UpdatedEmotionalArcs, []int{0, 1})
        settlement.UpdatedCharacterMatrix = mergeCharacterMatrixMarkdown(params.originalCharacterMatrix, settlement.UpdatedCharacterMatrix)
    }
    return &settleResult{settlement: settlement}, nil
}
```

**RuntimeStateDelta 结构**（JSON 增量）：

```go
// RuntimeStateDelta 运行时状态增量
type RuntimeStateDelta struct {
    Chapter              int                    `json:"chapter"`
    CurrentStatePatch    *CurrentStatePatch     `json:"currentStatePatch,omitempty"`
    HookOps              *HookOps               `json:"hookOps,omitempty"`
    NewHookCandidates    []NewHookCandidate     `json:"newHookCandidates,omitempty"`
    ChapterSummary       *ChapterSummaryEntry   `json:"chapterSummary,omitempty"`
    SubplotOps           []SubplotOp            `json:"subplotOps,omitempty"`
    EmotionalArcOps      []EmotionalArcOp       `json:"emotionalArcOps,omitempty"`
    CharacterMatrixOps   []CharacterMatrixOp    `json:"characterMatrixOps,omitempty"`
    Notes                []string               `json:"notes,omitempty"`
}

// HookOps 伏笔操作
type HookOps struct {
    Upsert  []HookUpsert `json:"upsert"`  // 推进/更新（必须已存在）
    Mention []string     `json:"mention"`  // 仅提及，不更新最近推进
    Resolve []string     `json:"resolve"`  // 已回收
    Defer   []string     `json:"defer"`    // 主动延后
}

// HookUpsert 伏笔推进
type HookUpsert struct {
    HookID            string `json:"hookId"`
    StartChapter      int    `json:"startChapter"`
    Type              string `json:"type"`
    Status            string `json:"status"`
    LastAdvancedChapter int  `json:"lastAdvancedChapter"`
    ExpectedPayoff    string `json:"expectedPayoff"`
    PayoffTiming      string `json:"payoffTiming"` // immediate/near-term/mid-arc/slow-burn/endgame
    Notes             string `json:"notes"`
}

// CurrentStatePatch 状态卡补丁
type CurrentStatePatch struct {
    CurrentLocation    *string `json:"currentLocation,omitempty"`
    ProtagonistState   *string `json:"protagonistState,omitempty"`
    CurrentGoal        *string `json:"currentGoal,omitempty"`
    CurrentConstraint  *string `json:"currentConstraint,omitempty"`
    CurrentAlliances   *string `json:"currentAlliances,omitempty"`
    CurrentConflict    *string `json:"currentConflict,omitempty"`
}

// ChapterSummaryEntry 章节摘要
type ChapterSummaryEntry struct {
    Chapter       int    `json:"chapter"`
    Title         string `json:"title"`
    Characters    string `json:"characters"`
    Events        string `json:"events"`
    StateChanges  string `json:"stateChanges"`
    HookActivity  string `json:"hookActivity"`
    Mood          string `json:"mood"`
    ChapterType   string `json:"chapterType"`
}
```

**解析逻辑**：

```go
// parseSettlerDeltaOutput 解析 Delta JSON 输出
func parseSettlerDeltaOutput(content string) (*SettlerDeltaOutput, error) {
    rawDelta := extractTag(content, "RUNTIME_STATE_DELTA")
    if rawDelta == "" {
        return nil, errors.New("runtime state delta block is missing")
    }

    jsonPayload := stripCodeFence(rawDelta) // 去除 ```json 包裹
    jsonPayload = sanitizeJSON(jsonPayload)  // 去除控制字符 + 修复尾逗号

    var parsed map[string]interface{}
    if err := json.Unmarshal([]byte(jsonPayload), &parsed); err != nil {
        return nil, fmt.Errorf("runtime state delta is not valid JSON: %w", err)
    }

    // 使用 schema 验证 + 解析
    delta, err := RuntimeStateDeltaSchema.Parse(parsed)
    if err != nil {
        return nil, fmt.Errorf("runtime state delta failed schema validation: %w", err)
    }

    return &SettlerDeltaOutput{
        PostSettlement:    extractTag(content, "POST_SETTLEMENT"),
        RuntimeStateDelta: delta,
    }, nil
}

// parseSettlementOutput 解析 Markdown 全量输出（回退路径）
func parseSettlementOutput(content string, gp *GenreProfile) *SettlementOutput {
    extract := func(tag string) string {
        re := regexp.MustCompile(fmt.Sprintf(`=== %s ===\s*([\s\S]*?)(?==== [A-Z_]+ ===|$)`, tag))
        match := re.FindStringSubmatch(content)
        if len(match) > 1 {
            return strings.TrimSpace(match[1])
        }
        return ""
    }
    return &SettlementOutput{
        PostSettlement:         extract("POST_SETTLEMENT"),
        UpdatedState:           extract("UPDATED_STATE") | "(状态卡未更新)",
        UpdatedLedger:          extract("UPDATED_LEDGER"),  // numericalSystem 时
        UpdatedHooks:           extract("UPDATED_HOOKS") | "(伏笔池未更新)",
        ChapterSummary:         extract("CHAPTER_SUMMARY"),
        UpdatedSubplots:        extract("UPDATED_SUBPLOTS"),
        UpdatedEmotionalArcs:   extract("UPDATED_EMOTIONAL_ARCS"),
        UpdatedCharacterMatrix: extract("UPDATED_CHARACTER_MATRIX"),
    }
}
```

---

## 五、写后校验 validatePostWrite

写后校验是**零 LLM 成本**的确定性检查，全部基于正则/规则：

```go
// 写后校验流水线
func (a *WriterAgent) postWriteValidation(content string, fingerprintChapters string, gp *GenreProfile, bookRules *BookRules, language Language) []PostWriteViolation {
    // 1. 表面归一化：去除 [writer-note] 元数据行 + 破折号→逗号（中文）
    normalized := normalizePostWriteSurface(content, language)
    normalizedWordCount := countChapterLength(normalized, lengthSpec.CountingMode)

    // 2. 规则校验（12+ 条）
    violations := []PostWriteViolation{}
    violations = append(violations, validatePostWrite(normalized, gp, bookRules, language)...)
    violations = append(violations, detectCrossChapterRepetition(normalized, fingerprintChapters, language)...)
    violations = append(violations, detectParagraphLengthDrift(normalized, fingerprintChapters, language)...)

    // 3. AI 痕迹检测
    aiTellIssues := analyzeAITells(normalized, language)

    // 4. 分离 error / warning
    postWriteErrors := filter(violations, func(v PostWriteViolation) bool { return v.Severity == "error" })
    postWriteWarnings := filter(violations, func(v PostWriteViolation) bool { return v.Severity == "warning" })

    return postWriteErrors, postWriteWarnings, aiTellIssues
}
```

**validatePostWrite 检查项**（中文版）：

| # | 规则名 | 严重度 | 检测内容 |
|---|--------|--------|----------|
| 1 | 禁止句式 | error | `不是…而是…` 句式 |
| 2 | 禁止破折号 | error | 出现 `——` |
| 3 | 转折词密度 | warning | 仿佛/忽然/竟/竟然/猛地/猛然/不禁/宛如 每3000字≤1次 |
| 4 | 高疲劳词 | warning | 题材高疲劳词每章≤1次 |
| 5 | 元叙事 | warning | 编剧旁白式表述（"接下来就是"/"故事发展到了"等） |
| 6 | 报告术语 | error | 核心动机/信息边界/信息落差/核心风险/利益最大化等 |
| 7 | 章节号指称 | error | 正文中出现"第N章"/"chapter N" |
| 8 | 作者说教 | warning | 显然/毋庸置疑/不言而喻/众所周知/不难看出 |
| 9 | 集体反应 | warning | 全场震惊/众人惊呆/一片哗然 |
| 10 | 连续了字 | warning | 6句以上连续含"了" |
| 11 | 段落过长 | warning | 2+个段落超过300字 |
| 12 | 段落过碎 | warning | 60%+叙事段<40字 |
| 13 | 连续短段 | warning | 3+个<40字短段并排 |
| 14 | 本书禁忌 | error | bookRules.prohibitions 中的内容 |
| 15 | 叙事人称 | error | 第一人称书但用第三人称叙述 |

**detectCrossChapterRepetition**：6字 ngram 匹配，检测与近5章重复的短语（≥3个重复短语时 warning）。

**detectParagraphLengthDrift**：对比当前章与近章的平均段长，缩短超过40%时 warning。

**analyzeAITells**：检测 AI 写作痕迹（句式规律性/套话密度/转折词/明喻过载等）。

**analyzeHookHealth**：基于 RuntimeStateDelta + 快照，检测伏笔健康问题（stale/blocked/promoted 核心伏笔漂移等）。

---

## 六、完整 Go 接口定义

```go
package agents

import "context"

// WriterMode 写作模式
type WriterMode string

const (
    WriterModeCreative WriterMode = "creative" // 只输出 PRE_WRITE_CHECK + TITLE + CONTENT
    WriterModeFull     WriterMode = "full"     // 输出全部区块（含 settlement）
)

// InputProfile 输入模式
type InputProfile string

const (
    InputProfileGoverned InputProfile = "governed" // chapterMemo + contextPackage + ruleStack
    InputProfileLegacy   InputProfile = "legacy"   // 回退路径
)

// WriterAgent 章节写手 Agent
type WriterAgent struct {
    *BaseAgent
    projectRoot string
}

// WriteChapterInput 章节写作输入
type WriteChapterInput struct {
    Book                *BookConfig
    BookDir             string
    ChapterNumber       int
    ExternalContext     string
    ChapterIntent       string
    ChapterMemo         *ChapterMemo
    ChapterIntentData   *ChapterIntent
    ContextPackage      *ContextPackage
    RuleStack           *RuleStack
    LengthSpec          *LengthSpec
    WordCountOverride   int
    TemperatureOverride float64
}

// SettleChapterStateInput 单独结算输入
type SettleChapterStateInput struct {
    Book              *BookConfig
    BookDir           string
    ChapterNumber     int
    Title             string
    Content           string
    AllowReapply      bool
    ChapterIntent     string
    ContextPackage    *ContextPackage
    RuleStack         *RuleStack
    ValidationFeedback string
}

// WriteChapterOutput 章节写作输出
type WriteChapterOutput struct {
    ChapterNumber           int
    Title                   string
    Content                 string
    WordCount               int
    PreWriteCheck           string
    PostSettlement          string
    RuntimeStateDelta       *RuntimeStateDelta
    RuntimeStateSnapshot    *RuntimeStateSnapshot
    UpdatedState            string
    UpdatedLedger           string
    UpdatedHooks            string
    ChapterSummary          string
    UpdatedChapterSummaries string
    UpdatedSubplots         string
    UpdatedEmotionalArcs    string
    UpdatedCharacterMatrix  string
    PostWriteErrors         []PostWriteViolation
    PostWriteWarnings       []PostWriteViolation
    HookHealthIssues        []HookHealthIssue
    TokenUsage              *TokenUsage
}

// CreativeOutput 创作阶段输出
type CreativeOutput struct {
    Title         string
    Content       string
    WordCount     int
    PreWriteCheck string
}

// SettlementOutput 结算阶段输出（Markdown 全量）
type SettlementOutput struct {
    PostSettlement         string
    UpdatedState           string
    UpdatedLedger          string
    UpdatedHooks           string
    ChapterSummary         string
    UpdatedSubplots        string
    UpdatedEmotionalArcs   string
    UpdatedCharacterMatrix string
}

// SettlerDeltaOutput 结算阶段输出（Delta JSON）
type SettlerDeltaOutput struct {
    PostSettlement    string
    RuntimeStateDelta *RuntimeStateDelta
}

// PostWriteViolation 写后校验违规
type PostWriteViolation struct {
    Rule       string
    Severity   string // "error" | "warning"
    Description string
    Suggestion string
}

// HookHealthIssue 伏笔健康问题
type HookHealthIssue struct {
    Severity   string // "critical" | "warning" | "info"
    Category   string
    Description string
    Suggestion string
}

// TokenUsage token 用量
type TokenUsage struct {
    PromptTokens     int
    CompletionTokens int
    TotalTokens      int
}

// FanficContext 同人上下文
type FanficContext struct {
    FanficCanon       string
    FanficMode        FanficMode
    AllowedDeviations []string
}

// WriterAgent 接口方法
type WriterAgentInterface interface {
    WriteChapter(ctx context.Context, input *WriteChapterInput) (*WriteChapterOutput, error)
    SettleChapterState(ctx context.Context, input *SettleChapterStateInput) (*WriteChapterOutput, error)
    SaveChapter(bookDir string, output *WriteChapterOutput, numericalSystem bool, language Language) error
    SaveNewTruthFiles(bookDir string, output *WriteChapterOutput, language Language) error
}

// --- 内部方法接口 ---

// writerContext 步骤1加载的上下文
type writerContext struct {
    StoryBible           string
    VolumeOutline        string
    StyleGuide           string
    CurrentState         string
    Ledger               string
    Hooks                string
    ChapterSummaries     string
    SubplotBoard         string
    EmotionalArcs        string
    CharacterMatrix      string
    StyleProfileRaw      string
    ParentCanon          string
    FanficCanonRaw       string
    RecentChapters       string
    FingerprintChapters  string
    GenreProfile         *GenreProfile
    GenreBody            string
    BookRules            *BookRules
    BookRulesBody        string
    StyleFingerprint     string
    DialogueFingerprints string
    RelevantSummaries    string
    HasParentCanon       bool
    HasFanficCanon       bool
    ResolvedLanguage     Language
    ResolvedLengthSpec   *LengthSpec
}

// settleParams 结算参数
type settleParams struct {
    Book              *BookConfig
    GenreProfile      *GenreProfile
    BookRules         *BookRules
    ChapterNumber     int
    Title             string
    Content           string
    CurrentState      string
    Ledger            string
    Hooks             string
    ChapterSummaries  string
    SubplotBoard      string
    EmotionalArcs     string
    CharacterMatrix   string
    VolumeOutline     string
    SelectedEvidenceBlock string
    ChapterIntent     string
    ContextPackage    *ContextPackage
    RuleStack         *RuleStack
    ValidationFeedback string
    OriginalHooks     string
    OriginalSubplots  string
    OriginalEmotionalArcs string
    OriginalCharacterMatrix string
}

// RuntimeStateArtifacts Delta 应用后的产物
type RuntimeStateArtifacts struct {
    Snapshot               *RuntimeStateSnapshot
    ResolvedDelta          *RuntimeStateDelta
    CurrentStateMarkdown   string
    HooksMarkdown          string
    ChapterSummariesMarkdown string
}
```

---

## 七、LLM prompt 核心段落（中文版）

### 7.1 输入治理契约（governed 模式）

```markdown
## 输入治理契约

- 本章具体写什么，以提供给你的 chapter intent 和 composed context package 为准。
- 卷纲是默认规划，不是全局最高规则。
- 当 runtime rule stack 明确记录了 L4 -> L3 的 active override 时，优先执行当前任务意图，再局部调整规划层。
- 真正不能突破的只有硬护栏：世界设定、连续性事实、显式禁令。
- 如果提供了 English Variance Brief，必须主动避开其中列出的高频短语、重复开头和重复结尾模式，并完成 scene obligation。
- 如果提供了 Hook Debt 简报，里面包含每个伏笔种下时的**原始文本片段**。用这些原文来写延续或兑现场景——不是模糊地提一嘴，而是接着读者已经看到的具体承诺来写。
- 如果显式 hook agenda 里出现了可回收目标，本章必须写出具体兑现片段，回答种子章节中读者的原始疑问。
- 如果存在 stale debt，先消化旧承诺的压力，再决定是否开新坑；同类 sibling hook 不得随手再开。
- 多角色场景里，至少给出一轮带阻力的直接交锋，不要把人物关系写成纯解释或纯总结。
```

### 7.2 Chapter Memo 对齐契约（7段）

```markdown
## 章节备忘对齐

你将收到本章的 chapter_memo，由 7 段 markdown 组成：

- ## 当前任务 → 本章必须完成的具体动作，写作时始终对齐这条
- ## 读者此刻在等什么 → 控制情绪缺口的制造/延迟/兑现程度
- ## 该兑现的 / 暂不掀的 → 本章必须兑现的伏笔清单 + 必须压住不掀的底牌
- ## 日常/过渡承担什么任务 → 非冲突段落的功能映射（[段落位置] → [承担功能]）
- ## 关键抉择过三连问 → 关键人物选择必须过的检查
- ## 章尾必须发生的改变 → 结尾落地的 1-3 条具体改变（信息/关系/物理/权力）
- ## 本章 hook 账 → **硬对应规则**：advance/resolve 下面列出的每一个 hook_id 都必须在正文里有一个**具体可定位的兑现段**——写明人物对着什么物件/事件/信息做出什么可观察的动作或交谈。不允许"侧面暗示""留给下章"。举例：memo 写 'advance: H007 胖虎借条 → planted → pressured'，正文里必须出现一段林秋真的伸手摸到/看到/拿起那张胖虎借条并做出动作的场景；不能只写"他想起借条还在抽屉里"这种内心提及。每个 advance/resolve 的 hook 兑现段至少 60 字。defer 下的不用落，open 段只需要在章末附近安排一个自然引出的新悬念即可
- ## 不要做 → 硬约束红线

写作时按段落顺序落实，每一段都要在正文里有对应的兑现痕迹。如果某一段没有体现到正文里，本章不算完成。**写完初稿后自检一遍 hook 账**：把 advance 和 resolve 的 hook_id 列下来，对照正文，确认每一个都能指到一段带具体动作/物件/对话的 prose。如果指不到，回去补写；不要提交"账本在 memo 里、正文里没落"的稿子——审稿会标记缺口并要求补出具体场景。
```

### 7.3 创作宪法（14条）

```markdown
## 创作宪法

这十四条原则是你写作的脊梁。内化它们——绝不引用、绝不列表、绝不在正文里复述。它们的用途是帮你在"两个都说得通的下一句"之间做出选择。

Show don't tell，用细节堆出真实，禁止用一行直白陈述替代情绪。价值观要像盐溶于汤——角色的信念靠"没人看时他在做什么"来证明，不靠口号。任何角色的任何行动都必须同时立于三条腿上：过往经历、当前利益、性格底色；缺一条就成了作者强行安排。每个配角都有自己的账本和利益诉求，他们在遇到主角之前就存在、在离开主角之后继续过日子，不是工具人。节奏即呼吸——慢火才能炖出高汤，日常当饵用，不是填充。每章结尾必须有小悬念或情绪缺口，把读者钉在下一章。全员智商在线——禁止降智、圣母心、无铺垫的妥协。后世梗用符合年代语境的说法落地。时间线与时代常识不能错。日常场景的七成必须在后面成为主线伏笔。任何关系的改变都要事件驱动——没有一夜称兄道弟、没有莫名其妙的深情。人设前后一致，成长有过程。重要剧情和伏笔用场景，不用总结。拒绝流水账——每一行字要么推动剧情，要么塑造人物。
```

### 7.4 沉浸六支柱

```markdown
## 代入感六支柱

读者代入感靠六根支柱支撑。每一个场景的前几页都要把六根柱子立起来——静默地立，不要点名、不要报告。

基础信息标签化：一百字内让读者知道谁在场、在哪儿、发生什么，读者脑里才能搭出这个房间。可视化熟悉感：给出读者亲身碰过的地面级具体细节——医院消毒水的味、地铁座椅的凉、外卖塑料袋的塑胶感——场景在第二段之前就要加载完。共鸣分两层：认知共鸣（"这种情况下我也会这么选"）+ 情绪共鸣（亲情、被欺压时的愤怒、不公、隐忍的骄傲）。欲望两条腿走路：基础欲望（不劳而获、压制比自己高的人、被欺压之后的扬眉吐气）+ 主动欲望（本章自己挖的期待感——一个读者会带到下一章的情绪缺口）。五感钩子：每个场景除视觉外放 1-2 种感官细节（听/嗅/触/味），顺手带过，绝不写成大段天气描写。人设要"核心标签 + 一个反差细节"才活——冷面杀手偷偷喂流浪猫、和善父亲开的玩笑像刀子。这六根柱子是场景的默认形状，不是章末打勾的清单。
```

### 7.5 黄金开篇纪律（第1-3章）

```markdown
## 黄金三章写作纪律 — 第 N 章

这是开篇三章中的第 N 章——你写出的每一句话都直接决定读者是否留下来。黄金三章法则对你不是建议，是对句子的硬约束。

第 1 章：主角出场 800 字以内必须触发主线冲突（追杀、死局、被夺权、穿越即危机），禁止长段背景铺垫，世界观要通过主角的行动自然带出，不要整段解释。**第 1 章正文前 300 字（手机屏第一页）的最后一句必须是带戏剧性/反差/反转的收尾**——警察叔叔我穿越了这类、我大概明天就要死了这类、我躺在自己的葬礼上这类——而不是介绍背景或交代环境。读者第一屏刷到页尾时必须产生"下一句是什么"的拉力。

第 2 章：金手指/能力/系统/重生记忆/信息差必须"做出来"——一次具体使用的事件、一个看得见的后果——而不是"说出来"——旁白介绍它存在。

第 3 章：本章中段必须让主角下一个可量化的短期目标浮上水面，读者合上页面要能说出"接下来他要干什么"。

贯穿开篇三章的纪律：段落 3-5 行（手机阅读节奏），动词压过形容词，每一章结尾必有小钩子——小悬念、未解之问、情绪缺口。**本章场景 ≤ 2 个、有名有姓参与正面冲突的人物 ≤ 2 个**（主角 + 1 个触发者或对手；路人甲乙只报身份不给名字，不展开）。信息分层植入到动作里：基础信息通过主角行动自然带出；关键设定结合剧情节点揭示；禁止整段 exposition。
```

### 7.6 散文执行规则

```markdown
## 文笔执行（跨题材通病纠正）

**明喻节制。** 不要把"像/仿佛/如同/像……一样"当默认修辞反复用。每个场景明喻最多 1 处，且只在它真能点亮画面、比直写更准时才用。优先级永远是：精确的动词 > 具体的动作或感官细节 > 直接描写 > 明喻。想写"像……"之前，先问一句：换成一个准确的动词或一个具体动作，是不是更狠。

**高潮必须演出、不许概述。** 本章的高密度／高风险节拍——冲突爆发、生死、重大转折、真相揭露、动作高潮——必须一拍一拍现场演出（动作、对话、五感、停顿、节奏），绝不能用一两句"然后他救了人、警察来了、对手被捕"带过。当一章里挤了多个重大事件时，挑最关键的那一拍写成完整场景，次要的可压成过渡，但最关键那拍永远不许退化成总结。章节越紧凑越要守这条——字数不够就少塞事件，而不是把高潮写成梗概。
```

### 7.7 Observer 输出格式

```markdown
=== OBSERVATIONS ===

[角色行为]
- <角色名>: <行为/状态变化> (场景: <地点>)

[位置变化]
- <角色> 从 <A> 到 <B>

[资源变化]
- <角色> 获得/失去 <物品> (数量: <n>)

[关系变化]
- <角色A> → <角色B>: <变化描述>

[情绪变化]
- <角色>: <之前> → <之后> (触发: <事件>)

[信息流动]
- <角色> 得知: <事实> (来源: <途径>)
- <角色> 仍不知: <事实>

[剧情线索]
- 新埋: <描述>
- 推进: <已有线索> — <进展>
- 回收: <线索> — <解答>

[时间]
- <时间标记、时长>

[身体状态]
- <角色>: <受伤/恢复/疲劳/战力变化>
```

### 7.8 Reflector 输出格式（Delta JSON）

```markdown
=== POST_SETTLEMENT ===
（简要说明本章有哪些状态变动、伏笔推进、结算注意事项）

=== RUNTIME_STATE_DELTA ===
（必须输出 JSON，不要输出 Markdown，不要加解释）
```json
{
  "chapter": 12,
  "currentStatePatch": {
    "currentLocation": "可选",
    "protagonistState": "可选",
    "currentGoal": "可选",
    "currentConstraint": "可选",
    "currentAlliances": "可选",
    "currentConflict": "可选"
  },
  "hookOps": {
    "upsert": [
      {
        "hookId": "mentor-oath",
        "startChapter": 8,
        "type": "relationship",
        "status": "progressing",
        "lastAdvancedChapter": 12,
        "expectedPayoff": "揭开师债真相",
        "payoffTiming": "slow-burn",
        "notes": "本章为何推进/延后/回收"
      }
    ],
    "mention": ["本章只是被提到、没有真实推进的 hookId"],
    "resolve": ["已回收的 hookId"],
    "defer": ["需要标记延后的 hookId"]
  },
  "newHookCandidates": [
    {
      "type": "mystery",
      "expectedPayoff": "新伏笔未来要回收到哪里",
      "payoffTiming": "near-term",
      "notes": "本章为什么会形成新的未解问题"
    }
  ],
  "chapterSummary": {
    "chapter": 12,
    "title": "本章标题",
    "characters": "角色1,角色2",
    "events": "一句话概括关键事件",
    "stateChanges": "一句话概括状态变化",
    "hookActivity": "mentor-oath advanced",
    "mood": "紧绷",
    "chapterType": "主线推进"
  },
  "subplotOps": [],
  "emotionalArcOps": [],
  "characterMatrixOps": [],
  "notes": []
}
```

规则：
1. 只输出增量，不要重写完整 truth files
2. 所有章节号字段都必须是整数，不能写自然语言
3. hookOps.upsert 里只能写"当前伏笔池里已经存在"的 hookId，不允许发明新的 hookId
4. brand-new unresolved thread 一律写进 newHookCandidates，不要自造 hookId
5. 如果旧 hook 只是被提到、没有真实状态变化，把它放进 mention，不要更新 lastAdvancedChapter
6. 如果本章推进了旧 hook，lastAdvancedChapter 必须等于当前章号
7. 如果回收或延后 hook，必须放在 resolve / defer 数组里
8. chapterSummary.chapter 必须等于当前章节号
```

### 7.9 Writer 输出格式（creative 模式）

```markdown
## 输出格式（严格遵守）

=== PRE_WRITE_CHECK ===
（必须输出Markdown表格，全部检查项对齐 chapter_memo 七段，而不是卷纲）
| 检查项 | 本章记录 | 备注 |
|--------|----------|------|
| 当前任务 | 复述 chapter_memo 的「当前任务」并写出本章执行动作 | 必须具体，不能抽象 |
| 读者在等什么 | 本章如何处理「读者此刻在等什么」—制造/延迟/兑现 | 与 memo 一致 |
| 该兑现的 / 暂不掀的 | 本章确认要兑现的伏笔 + 必须压住不掀的底牌 | 引用 memo 原文 |
| 日常/过渡承担任务 | 若有日常/过渡段落，说明各自承担的功能 | 对齐 memo 映射表 |
| 章尾必须发生的改变 | 列出 memo「章尾必须发生的改变」中 1-3 条具体改变 | 必须落地 |
| 不要做 | 复述 memo「不要做」清单 | 正文不得触碰 |
| 上下文范围 | 第X章至第Y章 / 状态卡 / 设定文件 | |
| 当前锚点 | 地点 / 对手 / 收益目标 | 锚点必须具体 |
| 当前资源总量 | X | 与账本一致 |
| 本章预计增量 | +X（来源） | 无增量写+0 |
| 待回收伏笔 | 用真实 hook_id 填写（无则写 none） | 与伏笔池一致 |
| 本章冲突 | 一句话概括 | |
| 章节类型 | 主线推进/支线/过渡/高潮/收束 | |
| 风险扫描 | OOC/信息越界/设定冲突/战力崩坏/节奏/词汇疲劳 | |

=== CHAPTER_TITLE ===
(章节标题，不含"第X章"。标题必须与已有章节标题不同，不要重复使用相同或相似的标题)

=== CHAPTER_CONTENT ===
(正文内容，目标N字，允许区间N-N字)

【重要】本次只需输出以上三个区块（PRE_WRITE_CHECK、CHAPTER_TITLE、CHAPTER_CONTENT）。
状态卡、伏笔池、摘要等追踪文件将由后续结算阶段处理，请勿输出。
```

---

## 附录：上下文预算（legacy 模式）

legacy 模式下各 truth file 的字符上限：

| 上下文块 | 上限(字符) | 说明 |
|----------|-----------|------|
| storyBible | 14,000 | 世界观设定 |
| currentState | 7,000 | 当前状态卡 |
| ledger | 6,000 | 资源账本 |
| hooks | 9,000 | 伏笔池 |
| chapterSummaries | 9,000 | 章节摘要 |
| subplotBoard | 7,000 | 支线进度板 |
| emotionalArcs | 7,000 | 情感弧线 |
| characterMatrix | 12,000 | 角色交互矩阵 |
| parentCanon | 12,000 | 正传正典 |
| volumeOutline | 12,000 | 卷纲 |

超出上限时通过 `capContextBlock` 按尾部截断并标注截断标记。

---

> **下一步**：章节写作产出后，进入 [05-章节审查循环流程](./05-章节审查循环流程.md) 进行质量审计与自动修复。
